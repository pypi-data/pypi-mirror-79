from setuptools import setup

setup(name='bigaussian_distributions',
      version='1.0',
      description='A package for calculating Gaussian or Binomial distributions on a described data',
      packages=['bigaussian_distributions'],
      zip_safe=False)
