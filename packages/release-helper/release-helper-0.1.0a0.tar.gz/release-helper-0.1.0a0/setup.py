#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['release_helper', 'release_helper.commands']

package_data = \
{'': ['*']}

package_dir = \
{'': 'src'}

install_requires = \
['click', 'packaging']

extras_require = \
{'docs': ['sphinx'], 'test': ['pytest', 'pytest-cov', 'pytest-xdist']}

entry_points = \
{'console_scripts': ['release-helper = release_helper.__main__:main']}

setup(name='release-helper',
      version='0.1.0a0',
      description='Command line helpers for preparing releases for Python packages.',
      author='Pradyun Gedam',
      author_email='mail@pradyunsg.me',
      url='https://github.com/pradyunsg/release-helper',
      packages=packages,
      package_data=package_data,
      package_dir=package_dir,
      install_requires=install_requires,
      extras_require=extras_require,
      entry_points=entry_points,
      python_requires='>=3.7',
     )
