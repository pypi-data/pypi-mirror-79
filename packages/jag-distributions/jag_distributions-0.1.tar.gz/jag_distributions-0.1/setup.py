from setuptools import setup

setup(name='jag_distributions',
      version='0.1',
      description='Gaussian & Binomial distributions',
      packages=['jag_distributions'],
      author='Jeroen Goddijn',
      zip_safe=False)
