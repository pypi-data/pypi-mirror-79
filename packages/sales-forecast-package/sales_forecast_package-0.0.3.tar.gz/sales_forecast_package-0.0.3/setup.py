from setuptools import setup

setup(name='sales_forecast_package',
      version='0.0.3',
      description='Preprocessing classes for transforming a time series forecasting problem into a regression problem',
      packages=['sales_forecast_package'],
      zip_safe=False)
