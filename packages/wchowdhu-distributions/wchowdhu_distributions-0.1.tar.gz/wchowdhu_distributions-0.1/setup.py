from setuptools import setup

setup(name='wchowdhu_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['wchowdhu_distributions'],
      zip_safe=False)
