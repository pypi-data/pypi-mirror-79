from setuptools import setup

setup(name='distributions-LamaTesting',
      version='0.1',
      description='Gaussian distributions',
      packages=['distributions-LamaTesting'],
      zip_safe=False)
