class FrameSettings(object):

    def __init__(self):
        pass

    def update(self):
        pass
        #print c4d.documents.GetAllAssets(c4d.documents.GetActiveDocument(), False, '')
        # doc =  c4d.documents.GetActiveDocument()
        # take_data = doc.GetTakeData()
        # current_take = take_data.GetCurrentTake() 
        # print current_take
        # render_data = current_take.GetEffectiveRenderData(take_data)
        # render_data = render_data[0] 
        # fps = doc.GetFps()
        # start_frame = render_data[c4d.RDATA_FRAMEFROM].GetFrame(fps)
        # end_frame = render_data[c4d.RDATA_FRAMETO].GetFrame(fps)
        # frame_step = render_data[c4d.RDATA_FRAMESTEP]
        # print start_frame, end_frame, frame_step

